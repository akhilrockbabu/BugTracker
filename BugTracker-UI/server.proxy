import { defineConfig } from 'vite';
import { ng } from '@analogjs/vite-plugin-ng';

export default defineConfig({
  plugins: [ng()],
  server: {
    port: 4200,
    proxy: {
      '/api': {
        target: 'https://localhost:7062',
        changeOrigin: true,
        secure: false,
      },
    },
  },
});
